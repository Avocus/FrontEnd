import { StatusProcesso, TipoProcesso, Especialidade } from "@/types/enums";

export { StatusProcesso, TipoProcesso, Especialidade };

export const STATUS_PROCESSO = Object.values(StatusProcesso);
export const TIPOS_PROCESSO = Object.values(TipoProcesso);
export const ESPECIALIDADES = Object.values(Especialidade);